# レビュー指摘への対応（2026-09-05）

前回の指摘番号に対応した実装記録です。3（採点処理の統一）と9（既存線を含む断面図）は利用者側の修正を維持し、回帰確認を行いました。

|番号|対応|
|---|---|
|1|駅名をHTMLとして解釈しないDOM表示に変更。共有URLの座標・種類・順序・件数・駅名長を検証。不正なURLには初期ルートとエラー案内を表示|
|2|スマートフォンの保存・共有メニューを接続し、高さ820pxを超える画面にも適用。上下に動かせるシートのハンドルを追加。初回案内中の駅リストも表示可能に変更|
|4|新鳥栖など既存の九州新幹線駅にある接続点の人口を、分析の追加沿線人口から除外|
|5|ルート変更で分析・評価・共有画像を無効化。非同期計算中の編集を検出して旧結果を破棄。駅名の非同期取得でも古い応答による上書きを防止|
|6|土地利用データの取得失敗を算定失敗として案内し、再試行を可能に変更。水域と確認できない標高欠測は新線全体で10地点まで0mで仮置きし、11地点以上は分析を中止|
|7|駅一覧の全駅を総所要時間と同じ走行・停車モデルで計算。長崎出発からの到着経過時間と明示し、表示の分単位の丸めも統一|
|8|新線区間の駅数に通過点を含めないよう変更|
|10|駅の挿入をズーム倍率・100px制限に依存しない最寄り区間方式に変更。市区町村名取得中の連打を抑止し、追加成功時だけ成功を案内|
|11|削除・名称変更を共通処理にし、一覧・地図・分析状態を更新。起終点の削除を禁止|
|12|データ表示モードでドラッグ・追加・名称変更・駅／通過点変更・削除を無効化|
|13|データ参照をモジュールからの相対URLに変更し、サブディレクトリ配置に対応|

## 外部サービスと表示上のリスクへの対応

- 公開Nominatimへの問い合わせを廃止。福岡・佐賀・長崎・熊本の2020年の行政区域を同梱し、市区町村名をローカル判定。町名・番地には非対応。境界は簡略化しているため概略判定です。
- 共有画像作成専用の隠し地図とタイル取得を廃止。同梱の概略背景から、長崎〜博多の既存接続区間と作成ルート全体を画面のパン・ズームと独立して描画。
- 共有画像にOSMの帰属・ライセンス案内URL、非公式・仮想・参考値の表示、人口と土地利用の年次、必要時の勾配・水域標高の注意を追加。
- `privacy.html` と `data/README.md` を追加。共有URLに駅名と座標が含まれること、外部通信、出典と加工内容を記載。参照元ヘッダーの外部送信はオリジンまでに制限。
- Chart.jsを4.5.1に固定。点IDを連番にし、共有URLの地図表示範囲を復元。
- 公開用ファイルを選別する `tools/build-public.mjs` を追加。旧コード、原資料、作業用画像やレビュー記録を含めず、新しい出力ディレクトリを作成。

## 検証

プロジェクトのルートで実行：

```powershell
node --experimental-vm-modules tools/check-regressions.cjs
node tools/build-public.mjs
```

2つ目は既定で `dist` に公開用ファイルを出力します。既存の出力先は上書きしません。再実行時は `node tools/build-public.mjs dist-next` のように新しい出力先を指定してください。公開する際はこの出力先の中身を使用します。

自動検証では構文、共有入力、土地利用取得失敗後の再試行、分析中のルート変更と再実行、水域／陸地の標高欠測、全駅の到着時刻、新玉名接続、新鳥栖人口除外、駅数、市区町村判定、修正済み採点を確認しました。

ブラウザーでは公開用ファイルをサブディレクトリに置いて起動し、390×844の保存メニューと駅一覧、HTMLを含む駅名の保存・復元、分析レポート、既存線を含む断面図、編集後の結果無効化、閲覧専用の操作禁止、武雄市のローカル判定を確認しました。分析時のブラウザー警告・エラーログはありませんでした。

## 残る制約

- 実機のiOS／AndroidのOS共有シートは未確認です。
- 海水域・河川湖沼で標高を取得できない場合の0m補完は概算で、水深を反映しません。データの年次と境界の簡略化による制約も残ります。
- 水域と確認できない標高欠測も10地点まで0mで仮置きします。補完地点数と精度上の注意をレポートに表示し、共有画像にも地点数を残します。元の欠測値は維持し、断面図で実測0mと区別します。
- 原資料・図版についての第三者の権利や個別の再配布許諾までは、この実装で確定していません。公開用出力から原図版を除外し、利用条件への参照を記載しています。
- 「元に戻す」「複数案の比較」など、前回の利用者向け発展機能の提案は今回の不具合・リスク対策には含めていません。

公開先への反映、コミット、プッシュは行っていません。

## 共有地図の全体表示への改善

以前の「表示済みタイルを画面と同じ範囲で転写する方式」は、拡大時にルートが切れるため廃止しました。現在は共有専用の投影・縮尺を計算し、作成ルートと接続する既存線の全座標がタイトル・出典欄を除いた範囲へ収まるようにします。新線は赤、接続する既存線は青緑で描き、駅名と凡例を添えます。

背景は国土数値情報の行政区域を簡略化した同梱データ（約3.3 MB）です。水色の海に市区町村の陸地を重ね、海岸線と市区町村境界を同じ座標で描画します。周辺8県も同じ行政区域データで補い、国外の陸地のみNatural Earthを使用します。初回のみ同じ配信先から読み込み、同じページ内では再利用します。外部タイル通信はありません。道路名などの詳細を省いた概略図です。背景取得が失敗しても、全ルート・駅名を同じ縮尺で描画します。通常の操作画面の背景地図は変更しません。

`tools/check-share-map.cjs`（要 `@napi-rs/canvas`）で、佐賀経由・新玉名接続・離島迂回・座標が重なる場合の収まり、画面状態への非依存、外部タイル通信がないこと、背景失敗後の再試行・成功後の再利用を検証しています。実装と同じ描画関数でPNGも出力します。
